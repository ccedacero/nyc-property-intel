[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'if echo \"$RAILWAY_SERVICE_NAME\" | grep -q \"cron\"; then uv run nyc-property-intel-sync; else uv run nyc-property-intel; fi'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

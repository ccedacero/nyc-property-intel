[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'if [ \"$RAILWAY_SERVICE_NAME\" = \"nyc-property-intel-cron\" ]; then uv run nyc-property-intel-sync; else uv run nyc-property-intel; fi'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
